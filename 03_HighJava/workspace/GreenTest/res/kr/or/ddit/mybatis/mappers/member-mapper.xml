<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

<insert id="insertMember" parameterType="MemberVO">
	insert into member (MEM_ID,MEM_PASS,MEM_NAME,MEM_BIR,MEM_MAIL,MEM_TEL,MEM_NICKNAME)
	values (#{mem_id}, #{mem_pass},#{mem_name},#{mem_bir},#{mem_mail},#{mem_tel},#{mem_nickname})
</insert>

<select id="selectMemberCountOne" parameterType="String" resultType="int">
	select count(*) from member where mem_id=#{mem_id} and mem_leaveyn = 'N'
</select>

<select id="selectMemberOne" parameterType="MemberVO" resultType="MemberVO">
	select * from member where mem_id=#{mem_id} and mem_leaveyn = 'N'
</select>

<update id="updateMemberOne" parameterType="MemberVO">
	update member set MEM_PASS = #{mem_pass}, MEM_NICKNAME=#{mem_nickname},MEM_TEL=#{mem_tel},MEM_MAIL=#{mem_mail} where mem_id=#{mem_id}
</update>

<update id="updateMemberLeave" parameterType="MemberVO">
	update member set MEM_LEAVEYN = 'Y' where mem_id=#{mem_id} and mem_pass=#{mem_pass}
</update>

<select id="selectMemberLogin" parameterType="MemberVO" resultType="MemberVO">
	select * from member 
		where mem_id=#{mem_id} and mem_pass=#{mem_pass} and mem_leaveyn = 'N'
</select>

<select id="selectMemberFineId" parameterType="MemberVO" resultType="MemberVO">
	select * from member 
		where mem_name=#{mem_name} and mem_mail=#{mem_mail} and mem_leaveyn = 'N'
</select>

<select id="selectMemberFindPass" parameterType="MemberVO" resultType="MemberVO">
	select * from member 
		where mem_id=#{mem_id} and mem_mail=#{mem_mail} and mem_leaveyn = 'N'
</select>




</mapper>

